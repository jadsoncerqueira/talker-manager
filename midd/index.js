const validationId = require('./validationId.js');
const validationCredentials = require('./validationCredentials.js');
const validateEmail = require('./validationEmail');

module.exports = {
  validationId,
  validationCredentials,
  validateEmail,
};